import React from 'react';
import ServiceCard from './ServiceCard';

const ExampleCarouselImage = ({ service }) => {
  return (
    <ServiceCard service={service} />
  );
};

export default ExampleCarouselImage;
